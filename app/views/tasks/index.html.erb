<h1>The tasks index</h1>

<% @tasks.each do |task| %>
<ul>
  <li><%= task.title %></li>
  <%= link_to "#{task.title}", task_path(task) %>
</ul>
<% end %>

<%= link_to "create a new task", tasks_new_path %>
